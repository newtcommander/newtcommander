# Tandem Commander release signing profile (feature 050).
#
# Single committed source of the signing identity. Certificate rotation is
# a one-line change: install the new certificate into the Windows
# certificate store and update 'thumbprint' below. The next signing run
# re-signs every artifact still carrying the old certificate.
#
# The private key never lives in the repository; the thumbprint only
# selects a certificate already installed in the maintainer's store.
# Consumed by sign_release.ps1 and setup\build_setup.cmd.

thumbprint = a3d05ccf5ca13eaff49cc7f64d1832f0e6ef6733
timestamp_url = http://time.certum.pl
